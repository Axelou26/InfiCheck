# Copier vers .env et adapter. Ne jamais committer de secrets.
# Une fois l'URL définie, rebuild l'app pour l'embarquer.
# Les mises à jour de base suivantes passent par ce manifeste, sans nouveau build store.

EXPO_PUBLIC_CONTENT_MANIFEST_URL=
